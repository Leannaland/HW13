<h1>Burgers!</h1>

<h2>Create a burger!</h2>

<ul>
  {{#each burger}}
    {{#unless yummy}}
      {{> burger/burger-block yummy=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers!</h2>

<ul>
  {{#each Burgers}}
    {{#if yummy}}
      {{> burger/burger-block yummy=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add Burgers</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add Burgers</button>
</form>